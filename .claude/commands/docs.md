# /docs — JSDoc + README хэсэг үүсгэх

Доорх функц/модулийн documentation үүсгэ:

1. **JSDoc** — Бүх public функцэд:
   ```js
   /**
    * @description Функцын тайлбар
    * @param {type} name - Параметрийн тайлбар
    * @returns {type} Буцаах утгын тайлбар
    * @throws {Error} Алдааны нөхцөл
    */
   ```

2. **README хэсэг** — Энэ module-ийн:
   - Зорилго (1-2 өгүүлбэр)
   - Ашиглах жишээ (code block)
   - Параметр хүснэгт

Монгол эсвэл Англи хэлээр бичиж болно.
